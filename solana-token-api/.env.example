# Example .env file - do NOT commit secrets to git
# Replace the placeholder values with your own before running the app

SOLANA_NETWORK=https://api.devnet.solana.com
# PAYER_SECRET_KEY should be a JSON array string of the secret key, e.g. [1,2,3,...]
PAYER_SECRET_KEY=

# Equinox / token-specific variables
EQUINOX_MINT_ADDRESS=
MINT_ADDRESS=
DISTRIBUTOR_WALLET=
